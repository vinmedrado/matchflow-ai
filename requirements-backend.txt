fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
pandas>=2.0.0
pyarrow>=14.0.0
requests>=2.31.0
pytest>=8.0.0
httpx>=0.26.0
python-dateutil>=2.8.2

scikit-learn>=1.3.0

joblib>=1.3.0
# Optional parquet engine fallback for environments that prefer fastparquet
fastparquet>=2024.2.0

gunicorn>=21.2.0

# Optional ML engines used when available; pipeline falls back safely when unavailable.
lightgbm>=4.0.0
xgboost>=2.0.0
rapidfuzz>=3.6.0

# Optional for real FlashScore browser scraping; provider degrades safely without browser installed.
playwright>=1.40
