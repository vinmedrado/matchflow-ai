import { useEffect,useState } from 'react';import { api } from '../api/client.js';import Loading from '../components/Loading.jsx';import ErrorState from '../components/ErrorState.jsx';
export default function DataQuality(){const [data,setData]=useState(null),[err,setErr]=useState(''),[loading,setLoading]=useState(true);useEffect(()=>{api.quality().then(r=>setData(r.data)).catch(e=>setErr(e.message)).finally(()=>setLoading(false))},[]);if(loading)return <Loading/>;if(err)return <ErrorState message={err}/>;return <><h1>Data Quality</h1><pre className="json">{JSON.stringify(data,null,2)}</pre></>}
