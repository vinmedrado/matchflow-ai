import { useEffect,useState } from 'react';import { api } from '../api/client.js';import StatusBadge from '../components/StatusBadge.jsx';import Loading from '../components/Loading.jsx';import ErrorState from '../components/ErrorState.jsx';
export default function SystemStatus(){const [data,setData]=useState(null),[err,setErr]=useState(''),[loading,setLoading]=useState(true);useEffect(()=>{api.status().then(r=>setData(r.data)).catch(e=>setErr(e.message)).finally(()=>setLoading(false))},[]);if(loading)return <Loading/>;if(err)return <ErrorState message={err}/>;return <><h1>System Status</h1><div className="status-row"><StatusBadge ok={data.dataset_available} label="Dataset"/><StatusBadge ok={data.quality_report_available} label="Quality Report"/><StatusBadge ok={data.ollama_available} label="Ollama"/></div><pre className="json">{JSON.stringify(data,null,2)}</pre></>}
